{
  "name": "discord-1337-bot",
  "version": "0.0.1",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"node server.js\" \"(cd additions && npm run start) & npm run start\""
  },
  "dependencies": {
    "concurrently": "^7.5.0",
    "eris": "^0.7.2"
  },
  "engines": {
    "node": "8.x"
  }
}
